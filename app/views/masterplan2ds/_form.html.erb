<%= form_with(model: masterplan2d) do |form| %>
  <% if masterplan2d.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(masterplan2d.errors.count, "error") %> prohibited this masterplan2d from being saved:</h2>

      <ul>
        <% masterplan2d.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
